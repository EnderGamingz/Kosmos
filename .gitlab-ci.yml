stages:
  - build
  - release

# Handled by Vercel
#kosmos_app_build_job:
#  stage: build
#  image: docker:latest
#  variables:
#    IMAGE_TAG: $CI_REGISTRY_IMAGE/app:$CI_COMMIT_TAG
#  rules:
#    - if: $CI_COMMIT_TAG
#  script:
#    - cd app
#    - cp .env.deploy .env
#    - sed -i "s#\[BASE_URL\]#$BASE_URL#g" .env
#    - sed -i "s#\[LOCAL_URL\]#$LOCAL_URL#g" .env
#    - sed -i "s#\[ALLOW_REGISTER\]#$ALLOW_REGISTER#g" .env
#    - sed -i "s#\[SYSTEM_MESSAGE\]#$SYSTEM_MESSAGE#g" .env
#    - sed -i "s#\[BUILD_ID\]#$CI_COMMIT_SHORT_SHA#g" .env
#    - sed -i "s#\[BUILD_TAG\]#$CI_COMMIT_TAG#g" .env
#    - docker build -t $IMAGE_TAG .
#    - echo "$CI_REGISTRY_PASSWORD" | docker login -u "$CI_REGISTRY_USER" $CI_REGISTRY --password-stdin
#    - docker push $IMAGE_TAG

kosmos_backend_build_job:
  stage: build
  image: docker:latest
  variables:
    IMAGE_TAG: $CI_REGISTRY_IMAGE/backend:$CI_COMMIT_TAG
  rules:
    - if: $CI_COMMIT_TAG
  script:
    - cd backend
    - docker build -t $IMAGE_TAG .
    - echo "$CI_REGISTRY_PASSWORD" | docker login -u "$CI_REGISTRY_USER" $CI_REGISTRY --password-stdin
    - docker push $IMAGE_TAG


kosmos_release_job:
  stage: release
  needs:
#    - kosmos_app_build_job
    - kosmos_backend_build_job
  image: registry.gitlab.com/gitlab-org/release-cli:latest
  rules:
    - if: $CI_COMMIT_TAG
  script:
    - echo "Running release job"
  release:
    tag_name: $CI_COMMIT_TAG
    name: 'Release $CI_COMMIT_TAG'
    description: 'Release created by release-cli in Pipeline'


